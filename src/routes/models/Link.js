'use strict';

module.exports = Link;

class Link {
    constructor(id, url, hash, startDate, endDate) {
            this.hashId = id,
            this.url = url,
            this.hash = hash,
            this.starDate = startate,
            this.endDate = endDate
    }
}